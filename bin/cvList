#!/bin/bash
# List all cv variants.

function listVariants
{
    cd cvData/variant
    ls -1 | sed 's/\.txt$//g'
}

function listSrc
{
    cd cvData
    find -type f | cut -b3- | grep ^src | sort
}

case "$1" in
    "variants")
        listVariants
    ;;
    "src")
        listSrc
    ;;
    *)
        listVariants
    ;;
esac

